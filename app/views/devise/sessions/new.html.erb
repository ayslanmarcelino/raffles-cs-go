<div class="container">
  <div class="row justify-content-center">
    <div class="col-xl-10 col-lg-12 col-md-9">
      <div class=" o-hidden border-0 shadow-lg my-5">
        <div class=" p-0">
          <div class="row">
            <div class="col-lg-6">
              <div class="p-5">
                <div class="text-center">
                  <h1 class="h4 mb-4 text-white"><%= t('.has_login') %></h1>
                </div>
                <div class="alert-login text-center text-white">
                  <% flash.each do |key, value| %>
                    <% unless key.to_s == 'timedout' %>
                      <div class="flash <%= key %>">
                        <%= value %>
                      </div>
                    <% end %>
                  <% end %>
                </div>
                <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: 'user' }) do |f| %>
                  <div class="form-group">
                    <%= f.email_field(:email, placeholder: t('.email'), class:'form-control form-control-user', autofocus: true) %>
                  </div>
                  <div class="form-group">
                    <%= f.password_field(:password, placeholder: t('.password'), class:'form-control form-control-user') %>
                  </div>
                  <button class="btn btn-orange btn-block">
                    <%= t('.enter') %>
                  </button>
                <% end %>
              </div>
            </div>
            <div class="col-lg-6 bg-gradient-pingo d-none d-lg-block">
              <div class="p-5">
                <div class="text-center">
                  <h1 class="h4 mb-4 text-white"><%= t('.no_login') %></h1>
                  <%= render "devise/shared/links" %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
